<div class="mx-auto text-center mb-0 py-8 md:py-12 lg:py-16 px-8 md:px-16 lg:px-24">
    <h1 class="mb-4 text-3xl font-medium tracking-tight leading-none text-gray-800 md:text-4xl lg:text-5xl">{{ page.title }}</h1>
    {% if page.header.subtitle %}
    <div class="font-light text-gray-500 sm:text-xl">{{ page.header.subtitle }}</div>
    {% endif %}
</div>
